name: custom work flow
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      -master
  workflow_dispatch:

jobs:     
  readfilecontent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: hello world
        run: |
          pwd
          ls -la
